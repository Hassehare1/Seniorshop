{
  "compilerOptions": {
    "target": "ES2017",
    "lib": ["dom", "dom.iterable", "esnext"],
    "allowJs": true,
    "skipLibCheck": true,
    "strict": true,
    // Fångar oanvända variabler, importer och parametrar — den vanligaste
    // formen av död kod, och den som lämnas kvar när ett flöde flyttas.
    // Slogs på 2026-09-04 efter att redigeringen flyttat till kundkortet och
    // lämnat en oanvänd prop bakom sig. Repot var redan rent; regeln håller
    // det så utan att någon behöver komma ihåg att svepa.
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noEmit": true,
    "esModuleInterop": true,
    // Testfilerna importerar med explicit .ts-ändelse — det krävs av
    // `node --test`, som kör TypeScript direkt utan byggsteg. Flaggan är
    // ofarlig här eftersom noEmit är satt: inget kompileras ut.
    "allowImportingTsExtensions": true,
    "module": "esnext",
    "moduleResolution": "bundler",
    "resolveJsonModule": true,
    "isolatedModules": true,
    "jsx": "react-jsx",
    "incremental": true,
    "plugins": [
      {
        "name": "next"
      }
    ],
    "paths": {
      "@/*": ["./src/*"]
    }
  },
  "include": [
    "next-env.d.ts",
    "**/*.ts",
    "**/*.tsx",
    ".next/types/**/*.ts",
    ".next/dev/types/**/*.ts",
    "**/*.mts"
  ],
  // Testfilerna ingår MED FLIT. De var tidigare exkluderade, vilket betydde
  // att ett test kunde sluta kompilera utan att någon märkte det förrän det
  // kördes — typkontrollen i bygget hoppade över dem helt.
  "exclude": ["node_modules"]
}
